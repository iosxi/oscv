# 変更履歴

版番号は `src/oscv.cs` の `App.Version` が唯一の出どころ。v1 から 1 ずつ上げる。

## v11

- **窓の縦幅が勝手に伸びるのを直した。** v10 でスリープ復帰後やディスプレイの
  抜き差し後に、窓の高さがキャプション 1 つぶん (100% で 31px 前後) 伸びることがあった
- v10 の窓は、**外から来た大きさの変更をそのまま受け入れていた** (実測。
  `SetWindowPos` で 250x239 → 250x270 にすると、そのまま 270 のままになる)。
  スタイルには `WS_CAPTION` が付いているのに `WM_NCCALCSIZE` で非クライアント領域を
  0 にしてあるので、「クライアント領域の大きさ + 枠」で窓を計算し直す相手が
  1 人でもいれば、ありもしないキャプションの高さが足された値が飛んでくる
- 伸ばしている当人は特定できていない (単純な最小化/復帰、`WM_DISPLAYCHANGE`、
  `WM_SETTINGCHANGE` などを直接投げても再現しなかった)。
  そこで**出どころを探すのをやめ、出口で毎回正しい大きさへ戻す**ことにした。
  `BuildUi` が決めた大きさを覚えておき、`WM_WINDOWPOSCHANGING` で書き戻す。
  誰が計算し直しても、窓が実際に変わる前に必ずここを通る
- 最小化・最大化そのもの (`SWP_STATECHANGED`) には手を出さないので、
  タスクバーからの最小化と復帰は今までどおり動く (実測で確認)
- あわせて `AutoScaleMode = None` にした。中身の大きさは `Sc()` で自前に決めており、
  WinForms に測り直させる理由がないため

## v10

- **見た目はそのままで、Windows から見て「普通のウィンドウ」になるようにした。**
  枠なし (`FormBorderStyle.None`) にしていたせいで、タスクバーのボタンで
  最小化できない、ウィンドウスナップ系の常駐ソフトが相手にしてくれない、
  といった不都合が出ていた
- スタイルに `WS_CAPTION` / `WS_SYSMENU` / `WS_MINIMIZEBOX` を足し、
  `WM_NCCALCSIZE` で非クライアント領域を 0 にした。タイトルバーも枠も
  描かれないので**見た目は 1px も変わらない**（v9 と画面を撮り比べて確認。
  差はバージョン文字列とピンの状態だけ）
- `WS_MAXIMIZEBOX` は逆に落とした。250px 幅の道具が上端ドラッグの
  Aero Snap で全画面になっても困るだけなので
- **窓の移動を OS の移動ループに任せた**（`WM_NCLBUTTONDOWN` + `HTCAPTION`）。
  自分で `Location` を書き換えていたため `EVENT_SYSTEM_MOVESIZESTART` が
  飛ばず、「ウィンドウが動き始めたこと」を見ている常駐ソフトから
  ドラッグが見えていなかった

## v9

- **モニターの数だけ列を横に並べるようにした。** 2 台なら窓の幅が 2 倍になり、
  左右それぞれの列でスライダーもプリセットも独立して動く。タブのような
  切り替えをやめ、1 つの窓の中で両方の設定が完結する
- 列ごとに見出し「画面 N」と状態ランプを出す（1 台のときは今までどおり
  ヘッダーのランプだけで、窓の大きさも v8 と同じ）
- 「画面」の切り替え行と、最後に選んだ画面を覚える `display=` は不要になったので廃止
- 答えない画面は列ごと沈めて、見出しに「応答なし」と出す
- 中身の作りも作り替えた。`Ddc` は「今の 1 台」を持つのをやめ、
  **画面番号で読み書きする**形にした（`Ddc.Read(num, code)`）。
  `Cfg` も同じく番号を受け取る。DDC は 1 本のシリアル線なので、
  列が増えてもワーカーは 1 本のまま、書きたい値を最優先で流す

## v8

- **プリセットと最終値を、モニターごとに完全に分けて保存するようにした。**
  v4〜v7 は「そのモニターの値がまだ無ければ番号なしの旧キーを使う」動きだったが、
  それだと 1 台目で育てた値が 2 台目の出発点になり、板ごとに分けている意味が
  なくなる。番号なしのキーへは落とさず、値を持たないモニターは初期値から始める
- **起動直後の UI 更新が丸ごと捨てられていたのを直した。** ワーカーからの
  `BeginInvoke` は窓のハンドルが出来る前だと例外になる。ハンドルが出来るのは
  `Application.Run` の中なので、1 回目の「実値の表示」「掴んだ画面番号の確定」
  「起動時ボタンの有効化」が落ちていた（窓を前面に出した時の 2 回目で
  追いついていたので気付きにくかった）。**画面番号が確定しないと ini のキーに
  番号が付かない**ため、プリセットを画面ごとに分ける今回の変更ではここが効く。
  ワーカーを起こす前にハンドルを作るようにした
- コマンドラインは一発勝負なので、画面を開けなかったら 120ms 置いて 1 度だけ
  出し直す（直前に別プロセスが DDC を叩いていると空振りすることがある）
- コマンドラインで**書いた値を読み返して確かめる**ようにした。`SetVCPFeature` は
  無視されても true を返し、DDC もたまに取りこぼす。窓を出さないぶん結果が
  見えないので、違っていたら一度だけ書き直す（1 台あたり約 0.5 秒）

## v7

- **コマンドラインから、画面とプリセットを組で指定できるようにした。**

  ```
  oscv.exe 1=弱 2=強
  ```

  組をいくつでも並べられ、書いた順に反映する。2 台を一度に切り替えられる
- **指定があるときは窓を出さず、反映して終わる。** ショートカットやホットキーに
  割り当てて使う想定。指定なしで実行すれば今までどおり窓が開く
- プリセットは 弱 / 中 / 強（`1` / `2` / `3` でも可）。値は GUI と同じ ini から
  画面ごとに読む。ini には何も書き戻さない
- 終了コードは 0 = 全部反映、1 = 反映できなかった画面がある。
  指定を間違えたときだけ、使い方をダイアログで出す（窓なし exe なので
  コンソールには出せない）

## v6

- **「起動時」ボタンを追加した。** その画面を開いたときの値を自動で覚えていて、
  押すとそこへ戻る。触ったあとに元の状態が分からなくなるのを防ぐためのもの
- 値は自動で入るので、右クリックの保存は無い（弱 / 中 / 強 とはそこが違う）
- 画面ごとに持ち直す。切り替えた先の板を最初に読んだ値が、その板の戻り先になる
- ini には残さない。起動し直せば、その時点の実値がまた入る
- 実値を読めるまでは押せない（読めなかった画面では非活性のまま）

## v5

- **DDC に答えない画面のボタンを非活性にした。** 電源が落ちている、DDC/CI が
  切ってある、変換アダプタが DDC を通さない、といった画面を選んでも赤ランプに
  なるだけなので、選べないようにした（ツールチップに「応答なし」と出る）
- 判定は扱っている 3 つの VCP (`0x10` / `0x12` / `0xF9`) で、**どれか 1 つでも
  答えれば生きているとみなす**。読み取りは 2.5% で空振りするが、3 つ続けて
  空振りする確率は 1/64000 なので、複数試すこと自体がリトライになっている
- 調べるのは起動直後と、ウィンドウを前面に出したとき。前面に出したときは
  死んでいる画面だけ見に行くので、生きている画面に余計な往復をしない。
  消えていた画面の電源を入れてウィンドウをクリックすれば、また選べるようになる

## v4

- **モニターが 2 つ以上あるとき、1 台ずつ選んで調整できるようにした。**
  v3 までは物理モニターを探すのを 1 台目で打ち切っていたので、2 台目以降は
  存在しないのと同じだった
- ヘッダーの下に「画面」の切り替えを出す（モニターが 1 つなら今までと同じ窓）。
  番号は Windows のディスプレイ番号、並びは左右の並び順
- 最終値とプリセットをモニターごとに保存する。ini のキーは `m2.last0` のように
  画面番号付き（この時点では番号無しの旧キーを出発点にしていた。v8 でやめた）
- 最後に選んでいたモニターを次回の起動でも選ぶ
- その板が持っていない項目は沈めて触れなくする（LG 以外での
  ブラックスタビライザーなど）。書きに行って赤ランプになるのをやめた

## v3

- アプリのアイコンを付けた。エクスプローラーやショートカットに出る exe の顔と、
  ウィンドウ・タスクバー・Alt+Tab に出るアイコンの両方。
  元絵は `assets/icon_SRC.png`、ico の作り直しは `tools/make-icon.ps1`
- ico を埋め込んだぶん実行ファイルが 26KB → 108KB になった

## v2

- 名前の表記を全て小文字に統一（`oscv.exe`、`dist\oscv-v2.zip`、
  ウィンドウのヘッダー表示）
- 項目名の文字色を明るくした。背景に対するコントラスト比が 4.4:1 で
  AA 基準（4.5:1）に届いておらず、暗くしたときに読みづらかった。約 9.7:1 に変更
- 最前面固定のボタンを ◆ / ◇ から押しピンの絵に変更。
  白く塗って斜め＝留めている、輪郭だけで真横＝抜いている
- ウィンドウの横幅を 280 → 250（論理px）に縮めた

## v1

初回リリース。

- 明るさ / コントラスト / ブラックスタビライザーの 3 項目をスライダーで調整
- Windows 標準の DDC/CI (`dxva2.dll`) でモニターと直接やり取りし、
  LG の OnScreen Control もサービスも使わない
- ホバー＋ホイール、ドラッグ、プリセット（弱 / 中 / 強）に対応。
  キーボード入力と確認ダイアログは無し
- 配布用の zip を `dist\oscv-<版>.zip` に作る
