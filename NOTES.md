# 開発メモ

ビルド手順と、実装で必要になった実測値・踏んだ罠の記録。使うだけなら読まなくてよい。

## ビルドと配布

```
powershell -ExecutionPolicy Bypass -File build.ps1
```

`build.bat` をダブルクリックしても同じ（中で build.ps1 を呼ぶだけ）。
.NET SDK 不要。Windows 同梱の `csc.exe`（.NET Framework 4）だけでビルドできる。
ソースは [src/oscv.cs](src/oscv.cs) 1 ファイル。

出来上がるもの:

| | |
|---|---|
| `oscv.exe` | そのまま動く実行ファイル（ショートカットの向き先） |
| `dist\oscv-<版>.zip` | 配布用。中は `oscv-<版>\` の 1 階層に `oscv.exe` と `README.txt` |

`dist/` はリポジトリに含めない。配布物は GitHub のリリースに添付する。
古い zip は新しい方から 3 個だけ残る。

### 版番号

**`src/oscv.cs` の `App.Version` が唯一の出どころ。** v1 から 1 ずつ上げる。
build.ps1 がここを読んで zip 名を決め、`README.txt` の `@VERSION@` を置換する。
ウィンドウのヘッダーにも `oscv v2` として出る。

修正したら `App.Version` を 1 つ上げてからビルドする。

### ビルド時のガード

古いバイナリを新しい版番号で配ってしまう事故を防ぐため、build.ps1 は次を確認する:

- oscv.exe が起動中なら中断する（起動中の exe は上書きできない）
- ビルド後、exe の更新時刻が開始時刻より古ければ中断する

## アイコン

| | |
|---|---|
| `assets/icon_SRC.png` | 元絵 (1254x1254)。描き直したらこれを差し替える |
| `assets/oscv.ico` | 元絵から作った ico。生成物だがコミットしてある |
| `tools/make-icon.ps1` | ico を作り直すスクリプト。**絵を差し替えたときだけ**実行する |

ビルド時には走らない。`build.ps1` は出来上がった ico を 2 か所に埋めるだけ:

- `/win32icon:` … エクスプローラー・ショートカット・タスクバーのピン留めに出る exe の顔
- `/resource:assets\oscv.ico,oscv.ico` … 実行時にウィンドウとタスクバーへ出すもの。
  `MainForm.SetAppIcon` が読む

**同じ ico が 2 回入る。** 片方だけだと、exe のアイコンは出るがウィンドウは
既定のアイコンのまま（またはその逆）になる。

### 元絵は透明ではない

`icon_SRC.png` は 24bpp で、角丸四角の外側が**黒で塗り潰されている**。そのまま
ico にすると、アイコンの周りに黒い四角が出る。`make-icon.ps1` が角丸のマスクを
自前でかけて外側を透明にしている。閾値で走査した実測値:

- 角丸四角の範囲 = (71,71) から 1112x1112
- 角の半径 = 約 229px（辺の 0.206）。円弧で当てはめて誤差 1px 以内

縮小してからマスクをかけている。順序が逆だと、GDI+ が透明部分の黒を
混ぜ込んで縁に黒い輪郭が出る。

### ico に入れる大きさ

16 / 24 / 32 / 48 は無圧縮（32bpp BGRA）、64 / 128 は PNG 圧縮。

**48 以下を PNG にしてはいけない。** .NET Framework の `System.Drawing.Icon` は
PNG 圧縮された絵を選べず、無視する。ウィンドウ用の 16 と 32 はここから取るので、
PNG にすると `SetAppIcon` が絵を見つけられない。エクスプローラー（Windows 本体）は
PNG でも読めるので、大きい方だけ PNG にしてある。

256 は入れていない。元絵に粒状のノイズが乗っているぶん PNG が重く、256 を足すと
ico が 40KB → 98KB、exe が 108KB → 224KB になる。256 表示は Windows が 128 から
補間するので、そこは捨てた。

## モニター側の仕様（実測）

LG UN700 (LG HDR 4K) で確認した VCP コード:

| 項目 | VCP | write | read |
|---|---|---|---|
| 明るさ | `0x10` | 0〜100 | そのまま |
| コントラスト | `0x12` | 0〜100 | そのまま |
| ブラックスタビライザー | `0xF9` | **0〜20** | **write 値 x 5** |

`0xF9` は LG 固有コード。**write と read でスケールが違う**（`write 16` → `read 80`）。
GUI は 0〜20 に統一して表示している。

対応コードの一覧は `CapabilitiesRequestAndCapabilitiesReply` で取れる（約 4.4 秒）。
項目を追加するときは、まずこれで対応 VCP を確認し、write/read のスケールが
一致するか実測してから実装すること。

速度は読み書きとも**約 60ms**、ハンドル取得は 5ms。起動から実値表示まで約 250ms。

## 複数モニター

`EnumDisplayMonitors` が返す HMONITOR 1 個が Windows の画面 1 枚。そこから
`GetPhysicalMonitorsFromHMONITOR` で物理モニターを取る。画面番号は
`GetMonitorInfo` の `szDevice`（`\\.\DISPLAY2`）の末尾の数字で、Windows の
ディスプレイ設定に出る番号と同じ。ini のキーも列の見出しもこれを使う。

v3 までは**最初に物理モニターが取れた HMONITOR で打ち切っていた**ので、
2 台目以降は存在しないのと同じだった。

### 画面 1 台 = 列 1 本 (v9 から)

UI は `Column` が 1 台ぶんを丸ごと持ち（スライダー・プリセット・状態ランプ・
`avail` / `boot` などの状態）、画面の数だけ横に並べる。窓の幅は `ColW * 台数`。

`Ddc` は「今の 1 台」を持たない。**掴んだ板は全部掴んだまま**にして、
呼ぶ側が画面番号で指す（`Ddc.Read(num, code)` / `Ddc.Write(num, code, val)`）。
ハンドルを外に出さないのは、**0 が正当なハンドル値**で「未設定」と区別できないため。
番号 → 索引の解決は `IndexOf` が担い、見つからなければ -1 を返す。

**ワーカーは列が増えても 1 本のまま。** DDC は 1 本のシリアル線で、
2 スレッドから同時に叩くと両方失敗する。ループは
「どの列でもいいので書きたい値があればそれを最優先、無ければ読み直す」
という順で回す。読み取り中でも `form.AnyPending()` を見て、
どこかの列で操作が始まったら読むのをやめて書きに行く（ドラッグの追従を保つため）。

### ハンドルは「同時に開いていれば」別物

`GetPhysicalMonitorsFromHMONITOR` が返すハンドルは、1 台ずつ開いては壊すと
**どちらも 0 になる**（値が使い回される）。2 台同時に開いたままなら 0 と 2 で、
それぞれ正しい板に届く（実測）。

```
1 台ずつ開閉:  display1 -> h=0    display2 -> h=0     ← 同じ値。取り違える
同時に開く:    display1 -> h=0    display2 -> h=2     ← 別物として使える
```

**だから全部の画面を開いたまま持つ。** 掴み直すたびに値は変わる（次に開くと
3 になっていたりする）ので、ハンドルを ini などに覚えてはいけないし、
`Ddc` の外に出してもいけない（呼ぶ側は画面番号で指す）。

### 画面を指定したら、他の板に落ちない

コマンドラインで番号を指定したときは、その画面が答えなければ失敗させる
（`Cli.Ready`）。黙って別の板に書くと、指定したのと違うモニターの明るさが
変わってしまう。終了コード 1 の方がまだましなので、そうしてある。

### 答えない画面は触らせない

Windows が画面として並べていても、DDC で触れるとは限らない。電源が落ちている、
OSD で DDC/CI が切ってある、変換アダプタや KVM が DDC を通さない、など。
そういう画面は列ごと沈めて、見出しに「応答なし」と出す
（`Ddc.Alive` → `Column.SetLive`）。

判定は**扱っている 3 つの VCP を順に読み、どれか 1 つでも答えれば生き**とする。
読み取りは 2.5% で空振りするので 1 回の失敗では判断できないが、3 つ続けて
空振りする確率は 1/64000 なので、**複数のコードを試すこと自体がリトライになる**。
`Ddc.Read` のようなリトライ＋開き直しは要らない（開き直すと、他の列が掴んでいる
板まで一度落としてしまう）。

`Alive` は開いてあるハンドルで読むだけで、掴んでいる板には触らない。
呼ぶのは 2 か所:

| いつ | 対象 |
|---|---|
| 起動直後（実値を出した後） | 全画面 |
| ウィンドウを前面に出したとき | **死んでいる画面だけ** |

前面に出すたびに生きている画面まで叩くと、台数ぶんの 60ms を毎回捨てることに
なる。逆に死んでいる画面だけ見に行けば、モニターの電源を入れてウィンドウを
クリックするだけで復帰する。掴めている画面は読めている時点で生きているので、
そもそも調べない。

コストは、答えない画面 1 つにつき 3 回ぶんの読み取り（実測 約 190ms、電源が
落ちている板はタイムアウトぶん伸びる）。ワーカースレッドなので操作は止まらない。

画面の抜き差しで列の数は追随しない（`WM_DISPLAYCHANGE` は見ていない）。
起動が 250ms のツールなので、増減したら起動し直す方が早い。

### 対応していない項目の見つけ方

`0xF9` は LG 固有なので、他社の板では応答しない。実測した 2 台目
（1280x720 の外部ディスプレイ）は `0x10` `0x12` に答えて `0xF9` には答えなかった。

判定は ReadAll の中でやる。**同じ回に他の項目が読めていたら、線は生きている
＝その項目だけが無い**とみなして沈める。読み取りは 2.5% で失敗するので、
1 回の失敗では判断できない（`Ddc.Read` が 4 回リトライ＋開き直しまでやった
上での失敗を見ている）。

そのぶん、対応していない項目を 1 回目に読むのは高くつく（実測 約 0.9 秒）。
一度沈めれば以後は読みに行かないので、起動直後の 1 回だけ。

## コマンドライン

`oscv.exe 1=弱 2=強`。引数が 1 つでもあれば `Application.Run` を通らず、
`Cli.Run` が反映して終わる（`Main` が `int` を返すようになっている）。

窓なし exe (`/target:winexe`) なのでコンソールには何も出せない。
そのため**指定の間違いだけダイアログ**で知らせ、反映の失敗は終了コードと
デバッグログに任せる。ショートカットから使う道具なので、失敗のたびに
ダイアログが出る方が困る。

**書いた値は読み返して確かめる**（`Cli.WriteVerified`）。`SetVCPFeature` は
無視されても true を返すし、DDC 自体たまに取りこぼす。実際、続けて 2 組を
流したときに `0xF9` の書き込みが 1 回黙って消えた（再現はしなかった）。
GUI なら値が見えて押し直せるが、窓を出さないこの経路では気付けないので、
書く → 40ms 置く → 読む、で確かめて、違えば一度だけ書き直す。
そのぶん 1 台あたり約 0.5 秒かかる。

画面を開くのも一発勝負なので、失敗したら 120ms 置いて 1 回だけ出し直す。
GUI を起動したまま実行すると DDC がかち合うことがあり、実測でも
「別プロセスが叩いた直後の 1 回目」がよく空振りした。
GUI 側の表示は、次にウィンドウを前面に出したときに読み直されて追いつく。

## 実装上の罠

### SetVCPFeature は範囲外でも true を返す

`0xF9` に 25 や 90 を書いても、戻り値は true なのにモニターは黙って無視する。
これで一度「0xF9 は読み取り専用」と誤診した。実際は write スケールが 0〜20
だっただけ。**呼ぶ前に必ず clamp すること。** 戻り値は成否の判断に使えない。

### 書き込みも取りこぼす（v12 で再適用ボタンを付けた理由）

戻り値が true でも、書いた値が板に入らないことがある。実測で目立つのは
プリセットを押したとき（3 項目を続けて書くので、どれか 1 つが抜ける）。
読み取りの失敗と違って、書き込みは**失敗したことが分からない**のが厄介で、
表示だけが新しい値になり、画面は古いままになる。

同じ値をもう一度書けば直るが、GUI は「target == applied なら書かない」ので、
スライダーを別の値にしてから戻すしか手がなかった。v12 の再適用ボタンは
`applied` を `NotApplied`（`int.MinValue`）にして target と必ず食い違わせ、
同じ値でも書きに行かせている。

**確認は再適用のときだけ行う。** ドラッグ中の書き込みまで読み返すと、
1 回あたり 40ms の待ち + 60ms の読みが乗って追従が鈍るうえ、どうせ次の値で
上書きされるので確かめる意味がない。

原因そのもの（なぜ落ちるのか）は追っていない。

### 物理モニターのハンドルは 0 が正当な値

このマシンでは `GetPhysicalMonitorsFromHMONITOR` が返すハンドルが 0 だった。
`IntPtr.Zero` を「未オープン」の目印に使っていたため、正常なハンドルを
未初期化と誤判定して即座に諦めていた。**ハンドルの値で有無を判断しないこと。**
いまは `Ddc` の外にハンドルを出さず、画面番号 → 索引（見つからなければ -1）で
持ち回している。

### PHYSICAL_MONITOR は非 blittable

string を含むため、配列を `[Out] PHYSICAL_MONITOR[]` で受けると
ハンドルが正しく返らなかった。`AllocHGlobal` + `PtrToStructure` で
手動マーシャリングしている。

### 読み取りは一時的に失敗する

約 2.5%（1/40）の確率で `GetVCPFeatureAndVCPFeatureReply` が false を返す。
40ms 間隔でリトライすれば実質 0 になる（40 回試行して失敗 0）。

ただし**続けざまに 2 回空振りすることはある**（v12 の再適用を実測したとき、
`0xF9` の読み返しが 2 回連続で -1 を返し、3 回目で通った）。
`Ddc.WriteVerified` の試行回数を 2 → 3 にしたのはこのため。

### DDC は同時アクセスに弱い

I2C のシリアルバスなので、別プロセスが同じモニターを叩いていると読み書きが
失敗する。検証中、こちらのテストスクリプトとアプリ本体が競合して両方失敗した。
OnScreen Control を常駐させない方が安定する。

### 起動直後の BeginInvoke は投げられない

窓のハンドルが出来るのは `Application.Run` の中。ワーカーはコンストラクタで
起こしているので、**起動直後の 1 回目の `BeginInvoke` は例外**になり、
`catch { }` で黙って消えていた（実値の表示・掴んだ画面番号の確定・
「起動時」ボタンの有効化が全部これ）。

窓を前面に出したときの 2 回目で追いつくうえ、ini に前回の最終値を種として
入れているせいで**表示は正しく見えて**しまい、長く気付かなかった。
画面番号が確定しないと ini のキーに番号が付かないので、プリセットを画面ごとに
分けるようになって初めて実害が出た。

対策はワーカーを起こす前に `IntPtr hwnd = Handle;` でハンドルを作ること。
起動直後にワーカーから UI を触る処理を足すときは、ここが効いている。

### 枠なしウィンドウは「普通のウィンドウ」ではない

見た目のために `FormBorderStyle.None` にすると、Windows や常駐ソフトから見た
「ウィンドウらしさ」まで失う。実際に困ったのは次の 2 つ:

| 症状 | 原因 |
|---|---|
| タスクバーのボタンで最小化できない | `WS_MINIMIZEBOX` / `WS_SYSMENU` が無い |
| ウィンドウスナップ系の常駐ソフトが効かない | `WS_CAPTION` が無く、**移動が OS の移動ループを通らない** |

**対策 1: スタイルだけ普通のウィンドウにする。** `CreateParams` で
`WS_CAPTION | WS_SYSMENU | WS_MINIMIZEBOX` を足し、`WM_NCCALCSIZE` に 0 を
返して非クライアント領域を消す（＝キャプションも枠も描かれない）。
見た目は変わらないまま、扱いだけ普通のウィンドウになる。

- `ClientSize` で大きさを決めてはいけない。付けたスタイルからキャプションの高さを
  逆算されて窓が大きくなる。非クライアント領域が 0 なのだから `Size` を直に決める
- `MaximizeBox = false` は**枠なしのとき効かなかった**（実測。スタイルに
  `WS_MAXIMIZEBOX` が残る）。`CreateParams` で明示的に落とす。残っていると
  上端ドラッグの Aero Snap で全画面になってしまう

**対策 3 (v11): 大きさは毎回 `WM_WINDOWPOSCHANGING` で戻す。** 上の「`ClientSize` で
決めてはいけない」は自分のコードだけの話ではない。スタイルに `WS_CAPTION` が
付いている以上、**「クライアント領域の大きさ + 枠」で窓を計算し直す相手が
1 人でもいれば、ありもしないキャプションの高さ (100% で 31px 前後) が足された
大きさが飛んでくる**。実際に v10 で、スリープ復帰後やディスプレイの抜き差し後に
窓が縦に伸びた。

v10 の窓は外から来た大きさの変更をそのまま受け入れる。実測:

```
v10: SetWindowPos(h, 0,0, 250,270, SWP_NOMOVE) → 250x270 のまま
v11: 同じことをしても                          → 250x239 に戻る
```

**伸ばしている当人は特定できなかった。** 最小化/復帰 (`SW_MINIMIZE` → `SW_RESTORE`)、
`WM_DISPLAYCHANGE`、`WM_SETTINGCHANGE`、`WM_THEMECHANGED`、`WM_DPICHANGED` 相当を
直接投げても再現しない。候補は複数ある (最小化中に境界を触ると走る
`Form.RestoreWindowBoundsIfNecessary()` は `SizeFromClientSize()` を通すし、
OS 側にも画面構成の変化で窓を作り直す経路がある) が、**どれか 1 つを潰しても
残りが残る**。出どころを追うより出口を締める方が確実:

- `BuildUi` が決めた大きさを `fixedSize` に持っておく
- `WM_WINDOWPOSCHANGING` で `WINDOWPOS.cx/cy` を `fixedSize` に書き戻す。
  誰が計算し直しても、実際に窓が変わる前に必ずここを通る
- ただし `SWP_NOSIZE` と `SWP_STATECHANGED` (0x8000) の付いたものには手を出さない。
  最小化・最大化そのものを壊さないため。状態変化のあとに別の `SetWindowPos` として
  来る伸びは、そちらで捕まる
- ついでに `AutoScaleMode = None`。中身の大きさは `Sc()` で自前に決めているので、
  WinForms に測り直させる理由がない
- `debug.on` があると `size fixup 250x270 -> 250x239` と記録するので、
  何がどれだけ伸ばそうとしたかは後から見える

**対策 2: 移動は OS に任せる。** 掴んだら `ReleaseCapture()` してから
`WM_NCLBUTTONDOWN` に `HTCAPTION` を送る。自前で `Location` を書き換えると
`WM_ENTERSIZEMOVE` も `EVENT_SYSTEM_MOVESIZESTART` も発生しないため、
「ウィンドウが動き始めたこと」を監視している常駐ソフトから見えない。

検証は隣のプロジェクト `peta-magne`（ウィンドウ吸着ツール）のログで行った。
対策後、oscv のドラッグが `ドラッグ開始 hwnd=… size=624x327 pad=(0,0,0,0)` として
記録され、実際に 28px 吸着した。`pad=(0,0,0,0)` は不可視の余白が無いという意味で、
枠なしのままなので吸着はぴったり合う。

### PowerShell 5.1 と .ps1 の文字コード

BOM 無し UTF-8 の `.ps1` を ANSI として読むため、日本語コメントが化ける。
**`.ps1` は UTF-8 BOM 付きで保存すること**（`build.ps1`、`tools\make-icon.ps1`）。
`.bat` は ASCII のみにしてある。

化け方が厄介で、構文エラーになるとは限らない。コメント行の最後のバイトが
cp932 の 2 バイト文字の 1 バイト目として解釈されると、**行末の改行まで
飲み込まれて次の行がコメントの一部になる**。`make-icon.ps1` では
ビットマップを作る行が 7 行消えて、`$bmp` が null のまま使われ、
「LockBits に引数 3 個のオーバーロードは無い」という無関係なエラーが出た。
エラー行番号がソースとずれていたら、まずこれを疑う。

## osccli を使わなくなった経緯

当初は LG の `OSCCLI.exe` にコマンドを投げる実装だった。これは

- **1 コマンドあたり約 3.5 秒**かかる
- OnScreen Control が常駐していないと動かない
- 失敗してもエラー終了せず、文字列を返して正常終了する
  （成功 `Brightness value is : 42` / 失敗 `OnScreen Control process is terminated.`）

という代物で、ドラッグ中は送信を抑制する、デバウンスする、OSC を自動起動・
復旧する、といった対処が必要だった。

調べたところ osccli は DDC/CI をそのまま中継していただけで、独自機能は
何もなかった。`OSCCLI.exe` 自体に DDC/I2C のコードはなく（IPC クライアント）、
実際の I2C は `RHubLib.dll` が `DeviceIoControl` で行っている。

DDC/CI に切り替えた結果、書き込みが 3500ms → 60ms になり、上記の対処は
すべて不要になった。LG のプロセス・サービスを全停止した状態で動作確認済み。
